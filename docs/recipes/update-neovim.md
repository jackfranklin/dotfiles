# Update Neovim

## Script

1. Check for updates and review the plan:
   ```bash
   bash ~/dotfiles/scripts/update-neovim.sh --check
   ```
2. Explain the detected installation and planned update, then obtain explicit confirmation.
3. Only after confirmation, execute the update:
   ```bash
   bash ~/dotfiles/scripts/update-neovim.sh
   ```
