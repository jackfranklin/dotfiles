# Load the base config (compatible across versions)
run-shell 'tmux source-file "#{d:current_file}/tmux.base.conf"'

# Newer settings (e.g., tmux >= 3.3)
# Required by Claude Code: passthrough for escape sequences it sends direct
# to the terminal.
set -g allow-passthrough on
